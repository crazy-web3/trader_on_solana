# 🚀 前端快速开始

## 📋 5分钟快速上手

### 1️⃣ 安装依赖 (1分钟)

```bash
cd frontend
npm install
```

### 2️⃣ 启动后端服务 (1分钟)

在另一个终端:

```bash
# 回到项目根目录
cd ..

# 启动后端
python3 app.py
```

后端运行在 `http://localhost:5001`

### 3️⃣ 启动前端开发服务器 (1分钟)

```bash
# 在 frontend 目录
npm run dev
```

前端运行在 `http://localhost:3000`

### 4️⃣ 打开浏览器 (1分钟)

访问 `http://localhost:3000`

### 5️⃣ 开始使用 (1分钟)

- 📈 **行情数据** - 查看实时K线
- 🎯 **策略回测** - 快速回测策略
- 🔍 **完整回测** - 历史数据回测
- ⚡ **参数优化** - Grid Search 优化

---

## 🎯 常见操作

### 查看行情数据

1. 点击左侧 "📈 行情数据"
2. 选择币种 (BTC/USDT, ETH/USDT 等)
3. 选择时间周期 (1h, 4h, 1d 等)
4. 输入天数 (1-365)
5. 点击 "🔍 查询数据"

### 运行策略回测

1. 点击左侧 "🎯 策略回测"
2. 配置参数:
   - 币种: BTC/USDT
   - 模式: Long (做多)
   - 下限价格: 87000
   - 上限价格: 90000
   - 网格数量: 10
   - 初始资金: 10000
   - 回测天数: 7
3. 点击 "🚀 开始回测"
4. 查看结果和权益曲线

### 运行完整回测

1. 点击左侧 "🔍 完整回测"
2. 配置参数:
   - 币种: BTC/USDT
   - 模式: Long
   - 价格范围: 40000-60000
   - 网格数量: 10
   - 初始资金: 10000
   - 日期范围: 选择开始和结束日期
3. 点击 "🚀 开始回测"
4. 查看详细的性能指标

### 参数优化

1. 点击左侧 "⚡ 参数优化"
2. 配置基础参数
3. 设置参数范围:
   - 网格数量: 5,10,15,20
   - 下限价格: 38000,40000,42000
   - 上限价格: 58000,60000,62000
4. 选择优化指标 (总收益率、年化收益等)
5. 点击 "⚡ 开始优化"
6. 查看最优参数和结果对比

---

## 📊 界面说明

### 侧边栏导航
- 📈 行情数据 - 实时K线查看
- 🎯 策略回测 - 快速回测
- 🔍 完整回测 - 历史回测
- ⚡ 参数优化 - Grid Search

### 状态指示
- 🟢 绿色 - API 正常
- 🔴 红色 - API 异常
- ⏳ 加载中 - 正在处理

### 数据展示
- 📊 统计卡片 - 关键指标
- 📈 图表 - K线和权益曲线
- 📋 表格 - 详细数据

---

## 🔧 配置

### 修改 API 地址

编辑 `frontend/vite.config.js`:

```javascript
proxy: {
  '/api': {
    target: 'http://your-api-url:5001',  // 修改这里
    changeOrigin: true,
    rewrite: (path) => path
  }
}
```

### 修改端口

编辑 `frontend/vite.config.js`:

```javascript
server: {
  port: 3000,  // 修改这里
  host: '0.0.0.0'
}
```

---

## 🐛 常见问题

### Q: 连接失败怎么办？

A: 检查后端是否运行:
```bash
curl http://localhost:5001/api/health
```

如果返回 `{"status":"ok"}`，说明后端正常。

### Q: 图表不显示？

A: 
1. 检查数据是否加载成功
2. 打开浏览器开发者工具 (F12)
3. 查看 Console 标签是否有错误

### Q: 数据加载很慢？

A:
1. 减少查询天数
2. 检查网络连接
3. 检查后端服务性能

### Q: 如何修改默认参数？

A: 编辑各个组件文件:
- `frontend/src/components/StrategyBacktest.vue` - 策略回测默认值
- `frontend/src/components/FullBacktest.vue` - 完整回测默认值
- `frontend/src/components/ParameterOptimize.vue` - 参数优化默认值

---

## 📦 生产部署

### 构建生产版本

```bash
cd frontend
npm run build
```

输出到 `frontend/dist` 目录

### 启动生产服务器

```bash
npm run serve
```

访问 `http://localhost:3000`

### 使用 PM2 管理

```bash
# 安装 PM2
npm install -g pm2

# 启动应用
pm2 start server.js --name "trading-dashboard"

# 查看状态
pm2 status

# 查看日志
pm2 logs trading-dashboard

# 重启应用
pm2 restart trading-dashboard

# 停止应用
pm2 stop trading-dashboard
```

---

## 🚀 下一步

- 📖 阅读 `frontend/README.md` 了解更多
- 🌐 查看 `FRONTEND_DEPLOYMENT.md` 了解部署
- 💻 查看 `SYSTEM_COMPLETE.md` 了解整个系统

---

## 📞 需要帮助？

- 查看浏览器控制台错误信息
- 检查后端服务状态
- 查看相关文档

---

**版本**: 1.0.0  
**最后更新**: 2026-01-28

🎉 **祝你使用愉快！**
